int main() {
    int a = 1;
    int b = 2;

    /* Conflict: Does the 'elif' belong to the first 'if (a == 1)' or the second 'if (b == 2)'? 
       And in chained 'elif_list's, does the subsequent 'elif' bind to the current chain or a parent? */
    if (x == 1) pass;
    elif (x == 2) pass;
    elif (x == 3) pass;
    else pass;
                
    return 0;
}
